## Makefile.am

AM_CFLAGS = -O2 -Wall -Wextra -g -pedantic-errors -Wunused-parameter
sbin_PROGRAMS = zonemap

CFILES = control.c \
		  zonemap.c

HFILES = control.h \
		  zonemap.h

zonemap_SOURCES = ${CFILES} ${HFILES}
