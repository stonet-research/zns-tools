# Evaluate zns-tools performance

The `bench-zns-tools` script benchmarks the performance of `zns.segmap` (from `zns-tools.fs`).
It checks the performance of mapping 100, 1K, 10K, 25K, 50K and 100K respectively.
